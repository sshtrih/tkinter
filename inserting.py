def insert_category(collection, data):
    return collection.insert_one(data)


def insert_product(collection, data):
    return collection.insert_one(data)


def insert_order(collection, data):
    return collection.insert_one(data)


def insert_employee(collection, data):
    return collection.insert_one(data)


def insert_clients(collection, data):
    return collection.insert_one(data)
